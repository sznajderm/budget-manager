# Supabase Configuration
SUPABASE_URL=###
SUPABASE_KEY=###
SUPABASE_SERVICE_ROLE_KEY=###
# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=###

# Recommended: Set default model for structured output support
# Models that support JSON schema: anthropic/claude-3.5-sonnet, openai/gpt-4, openai/gpt-4-turbo
OPENROUTER_DEFAULT_MODEL=openai/gpt-4o-mini

# Optional: Override API base URL (default: https://openrouter.ai/api/v1)
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Optional: Override request timeout in milliseconds (default: 30000)
# OPENROUTER_TIMEOUT=30000

# Optional: Override max retry attempts (default: 3)
# OPENROUTER_MAX_RETRIES=3

# AI Suggestion mode
# Set to 'true' to run AI suggestion synchronously and include diagnostics in the response.
# Set to 'false' (or leave unset) to run AI suggestion asynchronously in background via waitUntil.
# AI_SUGGESTION_SYNC_MODE=false
